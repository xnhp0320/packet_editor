Design a simple parser to parse the text which describe a packet format.

- Use modern C++. Use C++ 20 aggressively.
- Use only STL, do not rely on boost. If you think boost is better, stop and ask.
- Try to use std::optional and other stuff for parser.
- Parser syntax, similar to scapy. e.g. PacketHeader(attribute=value, ...) / PacketHeader(attribute=value, ...) / ...

